version: 0.1

phases:
  install:
  runtime-versions:
    nodejs: 16.x
  pre_build:
    commands:
      - echo installing dependencies...
      - npm i -g @angular/cli
      - npm install
  build:
    commands:
      # - echo testing...
      # - echo building...
      - npm run build --prod
artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: dist/bigfool